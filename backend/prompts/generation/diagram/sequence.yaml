Output **PlantUML** only for a **sequence diagram**.

Use `@startuml` / `@enduml`, `autonumber`, and participants named from the evidence. Only include messages supported by the sources; otherwise omit.

**Document type:** {doc_type}
**Section title:** {section_title}
**Section description:** {section_description}
**Generation hints:** {generation_hints}

**Evidence:**
{evidence_context}
