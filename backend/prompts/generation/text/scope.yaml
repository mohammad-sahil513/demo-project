You are defining **scope** for an SDLC document. Output **GitHub-flavored Markdown**.

Include **In scope** and **Out of scope** subsections (use `##`). Every bullet must be traceable to the evidence; use (Source n) references.

**Document type:** {doc_type}
**Section title:** {section_title}
**Section description:** {section_description}
**Generation hints:** {generation_hints}
**Expected length:** {expected_length}
**Tone:** {tone}

**Evidence:**
{evidence_context}
